const {
    GraphQLObjectType,
    GraphQLString,
    GraphQLLint,
    GraphQLSchema,
    GraphQLList,
    GraphQLNonNull
} = require('graphql');

//root query
const RootQuery = new GraphQLObjectType({
    name:
});

module.exports = new GraphQLSchema({
  
});